import React, { Component } from 'react';
import * as d3 from "d3";

const TimeGraph = ({ chartData }) => {

    return <React.Fragment>
        {chartData[0].build}
    </React.Fragment>
}

export default TimeGraph;